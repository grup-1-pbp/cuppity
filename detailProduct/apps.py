from django.apps import AppConfig

class DetailProductConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'detailProduct'  # Ensure this matches your app folder name
